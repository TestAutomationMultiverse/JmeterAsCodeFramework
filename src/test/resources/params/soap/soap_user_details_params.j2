{
  "action": "GetUserDetails",
  "soapAction": "http://example.com/soap/GetUserDetails",
  "variables": {
    "userId": "${userId}",
    "requestId": "${requestId}",
    "timestamp": "${timestamp}"
  }
}