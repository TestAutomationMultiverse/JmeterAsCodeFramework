{
  "action": "CreateOrder",
  "soapAction": "http://example.com/soap/CreateOrder",
  "variables": {
    "customerId": "${customerId}",
    "productId": "${productId}",
    "quantity": "${quantity}",
    "orderDate": "${orderDate}",
    "requestId": "${requestId}"
  }
}