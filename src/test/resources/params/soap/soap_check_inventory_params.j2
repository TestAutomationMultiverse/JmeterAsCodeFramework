{
  "action": "CheckInventory",
  "soapAction": "http://example.com/soap/CheckInventory",
  "variables": {
    "productId": "${productId}",
    "warehouseId": "${warehouseId}",
    "requestId": "${requestId}",
    "timestamp": "${timestamp}"
  }
}